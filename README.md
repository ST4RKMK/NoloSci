


## First Start
l'applicativo si serve di npm per la parte di javascript, per il corretto funzionamento di npm è necessario andare a modificare, in base alla propria configurazione, i link di riferimento per lo script in app.blade e in guest.blade
